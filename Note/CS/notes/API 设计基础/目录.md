# API 设计基础 - 目录

这一组笔记讨论的是接口为什么不是“把数据吐出来就行”。好的 API 设计既影响调用方能否快速理解，也影响服务端能否长期演进。模块从 API 基本目标出发，依次覆盖 REST、JSON API、资源设计、HTTP 方法与幂等性、错误响应、分页过滤排序、版本化、OpenAPI，以及 Webhooks、SSE、WebSocket、gRPC、GraphQL 等扩展风格。

这组内容允许卡片化阅读，但每张卡片都尽量围绕一个判断展开，而不是写成规范清单。阅读时可以先抓主线：清晰、一致、可演进、边界明确；再去理解各种风格和协议为什么会出现。
